# Required
SECRET_KEY=

# Optional (defaults shown)
DATABASE_URL=sqlite:///tasks.db
DEBUG=false
HOST=0.0.0.0
PORT=5000
CORS_ORIGINS=*

# Auth
JWT_ALGORITHM=HS256
JWT_EXP_HOURS=1

# Email (notification service)
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
